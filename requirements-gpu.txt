# GPU-accelerated PyTorch (CUDA 12.6)
# Install with: pip install -r requirements-gpu.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# This replaces the CPU-only torch from requirements.txt.
# Install base deps first: pip install -r requirements.txt
# Then override torch: pip install -r requirements-gpu.txt --extra-index-url https://download.pytorch.org/whl/cu126

torch>=2.2.0
